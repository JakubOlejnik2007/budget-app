const urls = {
    client: {
        homepage: "/",
        about: "/o-aplikacji",
        login: "/logowanie"
    },
    backend: {
        auth: {
            register: "/register",
            login: "/login"
        }
    }
}

export default urls